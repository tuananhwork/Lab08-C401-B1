# Day 09 Lab — Multi-Agent Orchestration
# Install: pip install -r requirements.txt

# ─── Core ───────────────────────────────
python-dotenv>=1.0.0

# ─── Vector Store ───────────────────────
# chromadb already installed from day08 venv (1.5.7)
# Reinstalling triggers onnxruntime dep which has no Python 3.14 wheel
# chromadb==1.5.7

# ─── Embeddings ─────────────────────────
# sentence-transformers requires torch which has no Python 3.14 wheel
# Use OpenAI embeddings instead (EMBEDDING_PROVIDER=openai in .env)
# sentence-transformers>=2.6.0

# ─── LLM ────────────────────────────────
# Option A: OpenAI
openai>=1.12.0

# Option B: Google Gemini
# google-generativeai>=0.5.0

# ─── Orchestration ──────────────────────
# Option: LangGraph (uncomment nếu dùng LangGraph)
# langgraph>=0.1.0
# langchain>=0.2.0
# langchain-openai>=0.1.0

# ─── Data ───────────────────────────────
pyyaml>=6.0.1

# ─── MCP (optional — Sprint 3 Bonus) ───
# Uncomment nếu muốn implement real MCP server:
# mcp>=1.0.0
# httpx>=0.27.0
# fastapi>=0.110.0
# uvicorn>=0.27.0

# ─── Dev/Test ───────────────────────────
pytest>=8.0.0
